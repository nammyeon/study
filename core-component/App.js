import { View, Text } from "react-native";

export default function App() {
    return (
        <View style={{ flex: 1, backgroundColor: "white", paddingTop: 100 }}>
            <Text style={{ fontSize: 20, fontWeight: "bold", color: "black", textAlign: "center" }}>Hello World</Text>
        </View>
    );
}
